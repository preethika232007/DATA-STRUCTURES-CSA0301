graph = {
    'A': ['B', 'C'],
    'B': [],
    'C': []
}

visited = []

def dfs(node):
    if node not in visited:
        print(node)
        visited.append(node)
        for i in graph[node]:
            dfs(i)

dfs('A')
